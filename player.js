// Class definition for Player

class Player {
  constructor(name, id, scores = []) {
    this.name = name;
    this.id = id;
    this.scores = scores;
  }
}

// Function to add a new player
function addPlayer() {}
